export default function OverviewComponent(){
    return(
        <section className={` p-10 w-full `}>
            <section className={` w-full flex flex-col gap-4 `}>
                <section className={` bg-accent flex w-full`}>


                </section>
            </section>
        </section>
    )
}